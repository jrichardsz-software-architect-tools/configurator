{
  "database": {
    "host":"${CONFIGURATOR_DATABASE_HOST}",
    "port":"${CONFIGURATOR_DATABASE_PORT}",
    "user":"${CONFIGURATOR_DATABASE_USER}",
    "password":"${CONFIGURATOR_DATABASE_PASSWORD}",
    "name":"${CONFIGURATOR_DATABASE_NAME}"
  },
  "api":{
    "key":"${API_KEY}"
  }

}
