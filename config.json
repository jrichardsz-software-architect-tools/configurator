{
  "allowDocker":"${ALLOW_DOCKER}",
  "database": {
    "host":"${CONFIGNET_DATABASE_HOST}",
    "port":"${CONFIGNET_DATABASE_PORT}",
    "user":"${CONFIGNET_DATABASE_USER}",
    "password":"${CONFIGNET_DATABASE_PASSWORD}",
    "name":"${CONFIGNET_DATABASE_NAME}"
  }
}
